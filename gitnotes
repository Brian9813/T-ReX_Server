Don't judge i'm new


to add to the commit use aliad -> gtad [filename]

to check status use alias -> gtstat

to make new commit use alias -> gtcom [commit notes]

To push to github use alias -> gtpush
